# Favorite Book - Create a function called favorite_book() and have it
    # accepts a parameter 'title'. The function should print a message 
    # "My favorite book is {title}" Make sure the message is grammatically correct. 

def favorite_book(title):
    """This function will take the input title and print out the favorite book of the user."""
    print(f"My favorite book is {title.title()}.")

favorite_book("Enders game")