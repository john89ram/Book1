#Change format types with method edits, additional information can be added inside the "()" after the .title.
name = "john ramirez"
print(name.title())
#Here a different method type is used to change the format of the string
print(name.upper())

print(name.lower())
#***lower is best for storing data***