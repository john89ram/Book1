# Here we will manipulate the range() method to make changes while working numbers

even_numbers = list(range(2,11,2))
    # Within the range we are stating that we are counting from "2" through "10" (off-by-one) but we are counting by "2s"
    # This will print out all even numbers from 2-10, this can also count all odd numbers by changing the first two integers in the range.
print(even_numbers)

