# Odd numbers - Use the 3rd argument in the range() function to make a list of odd numbers from 1 - 20. Use a for loop to print each number.
odd_numbers = list(range(1,22,2))
for odd_number in odd_numbers:
    print(odd_number)